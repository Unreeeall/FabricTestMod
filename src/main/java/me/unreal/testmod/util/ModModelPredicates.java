package me.unreal.testmod.util;

import me.unreal.testmod.TestMod;
import me.unreal.testmod.component.ModDataComponentTypes;
import me.unreal.testmod.item.ModItems;
import net.minecraft.client.render.item.model.special.SpecialModelTypes;
import net.minecraft.util.Identifier;
/*
public class ModModelPredicates {
    public static void registerModelPredicates() {
        ModelPredicateProviderRegistry.register(ModItems.CHISEL, Identifier.of(TestMod.MOD_ID, "used"),
                (stack, world, entity, seed) -> stack.get(ModDataComponentTypes.COORDINATES) != null ? 1f : 0f);
        SpecialModelTypes.ID_MAPPER(ModItems.CHISEL,)
    }
}

 */




